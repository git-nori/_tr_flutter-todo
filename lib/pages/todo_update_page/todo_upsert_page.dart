import 'package:flutter/material.dart';

class TodoUpSertPage extends StatelessWidget {
  static const routeName = '/list/update';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Todo Update'),
      ),
    );
  }
}
